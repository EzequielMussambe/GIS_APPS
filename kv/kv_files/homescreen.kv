#:kivy 1.11.1
#:import utils kivy.utils
#:import MapView kivy.garden.mapview.MapView
<HomeScreen>:
    MapView:
        canvas.before:
            Color:
                rgb:utils.get_color_from_hex("#ABD3DF") 
            Rectangle:
                pos:self.pos
                size:self.size
    #id:mains
        lat:10
        lon:10
        zoom:5
        double_tap_zoom:True
        #self.center_on(10,10)
        #on_lat:
        #shows.text=str(self.lat) +" , "+str(self.lon)
        MapMarkerPopup:
            #id:coors
            source:"images/pin.png"
            lat:10
            lon:10